let //previene la sobreescritura accidental de variables
const //prohibe la reasignación de valores a variables.

let cadena = 'carlos'; //string
var numero = 12; //number
var boolean = true; //boolean (true o false)
var esNull = null; //null
var persona = { nombre:'pedro',apellido:'madrigal',edad: 12}; //object
const teacherBirthYears = [1996,1986]; //los arrays son listado de elementos del mismo tipo

console.log( typeof(cadena));//typeof dice que tipo es la variable
console.log( persona.nombre);
console.log(teacherBirthYears);